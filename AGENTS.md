# AGENTS.md

## Layout

- Git repo root is `unity files/`, but the actual Unity project lives in `FALL/`. All Unity paths and commands belong under `FALL/`, not the repo root.
- Pinned Unity version: **6000.3.22f1** (`FALL/ProjectSettings/ProjectVersion.txt`). Do not open or upgrade with a different version.
- URP **2D** setup: `Assets/Settings/Renderer2D.asset` + `UniversalRP.asset`; only scene is `Assets/Scenes/SampleScene.unity`.
- `Assets/Scripts/` contains the player core: `KitType.cs`, `PlayerKit.cs`, `PlayerBehaviour.cs`, `PlayerAnimation.cs`, `PlayerControl.cs`.
- Player art lives in `Assets/Objects/Player/`: `Player.png`, one Animator controller `Player_0.controller`, and per-powerup clips named `<Powerup>_kit.anim` (Fragile, Ice, Invincible, Shooter, Slime, Slowfalling, Speed, Time). Follow that naming pattern for new powerup clips.
- Input System package is installed; actions asset is `Assets/Settings/InputSystem_Actions.inputactions`.

## Git state

- Branch `main` tracks `origin/main` (`https://github.com/Hamzaoui-Louai/FALL.git`).
- Ignore rules live in the root `.gitignore` (Unity template scoped with `FALL/` prefixes): `Library/`, `Temp/`, `Logs/`, `UserSettings/`, generated `*.sln*` / `*.csproj` are excluded; `.opencode/node_modules/` etc. are covered by `.opencode/.gitignore`.
- Intentionally tracked beyond Assets/Packages/ProjectSettings: root docs (`README.md`, `AGENTS.md`) and `FALL/.vsconfig` + `FALL/.vscode/` (editor/debug config; root `.gitignore` ignores `.vs/` but not `.vscode/`).
- The Unity editor holds `FALL/Temp/UnityLockfile` while open; never delete `Temp/` or `Library/` while it runs.

## Build / verification

- No CLI build/test pipeline exists. `FALL.slnx` is an editor-generated stub (empty `<Solution>` because there are no scripts); `.sln`/`.csproj` files are regenerated by Unity — do not hand-edit them and do not use `dotnet build` as verification.
- Verify by letting the Unity editor compile (check Console) or via Window > General > Test Runner (Test Framework package is installed; no tests exist yet).
- When creating/moving assets under `Assets/`, keep each file's `.meta` sibling paired (GUIDs); prefer doing moves inside the editor.

## Code conventions

- **Public functions set state; logic runs in FixedUpdate/Update.** Expose
  multi-value setters (e.g. `RotateRight()` sets direction + speed), but the
  actual behavior (rotation, animation, timer countdown) lives in the frame
  loop. This keeps classes decoupled and testable.
- **Single responsibility per script:**
  - `PlayerControl` — Input proxy. Reads keyboard, calls 3 commands on
    `PlayerBehaviour`. Never touches animation or kit.
  - `PlayerBehaviour` — Physics + state holder. Owns kit, exposes direction/speed
    for animation, applies rotation in FixedUpdate.
  - `PlayerKit` — Pure state class. Holds kit type + countdown timer. Not a
    MonoBehaviour.
  - `PlayerAnimation` — Animator driver. Reads behaviour state + kit in
    FixedUpdate, sets Animator speed and state. Does not set kit.
- **Intent-revealing names:** `RotateRight()`, `ApplyKit()`, `Tick()` — not
  `Rotate()`, `DoKit()`, `UpdateTimer()`.
- **Enums for state:** Use `PlayerDirection`, `KitType` instead of magic
  strings or ints.
- **No direct input in Behaviour:** Only `PlayerControl` reads input.
  `PlayerBehaviour` is input-agnostic.
- **No abbreviations in variable or parameter names:** Write `keyboard`, not
  `kb`. Write `behaviour`, not `bhv`. Readability over brevity.
- **Do not multiply by `Time.fixedDeltaTime` inside `FixedUpdate`.** The time
  step is already constant — the multiplication is redundant.

## Other instruction sources

- Repo-local skills live in `.opencode/skills/` (`unity-developer` is the relevant one for Unity work).
